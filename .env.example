# .env.example
# Copy this file to .env and fill in the required values.

# The absolute path to the root directory of this project.
# This is crucial for resolving paths to data and configuration files.
# Example (Linux/macOS): /Users/username/Projects/solar-irradiance-from-ac-export
PROJECT_ROOT=

# The name of the configuration file within the PROJECT_ROOT.
CONFIG_FILENAME=config.yml

# --- Data File Configuration (Long Format) ---

# The ABSOLUTE path to the CSV file containing the hourly AC export data
# and market spot prices.
#
# REQUIRED CSV FORMAT (Long/Tidy Format):
# 1. Datetime Column: 'timestamp_utc' (hourly, UTC timezone).
# 2. Park Identifier Column: 'park_name' (must match keys in config.yml).
# 3. Production Column: 'ac_export_kwh' (kilowatt-hours).
# 4. Price Column: 'spot_price_eur_mwh' (EUR/MWh, excluding VAT).
#
# Note: The price column must contain the specific spot price relevant to the
# park and hour in that row.
PRODUCTION_AND_PRICE_FILE_PATH=

# --- Weather Data Configuration ---

# The ABSOLUTE path to the CSV file containing the hourly weather data.
#
# REQUIRED CSV FORMAT:
# 1. Datetime Column: 'timestamp_utc' (hourly, UTC timezone).
# 2. Weather Columns:
#    - 'temp_air_c' (Air temperature in °C)
#    - 'wind_speed_m_s' (Wind speed in m/s)
#    - 'pressure_hpa' (Atmospheric pressure in hPa)
#    - 'ghi_w_m2' (Global Horizontal Irradiance in W/m^2)
WEATHER_FILE_PATH=